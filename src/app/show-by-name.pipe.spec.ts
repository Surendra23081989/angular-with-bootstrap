import { ShowByNamePipe } from './show-by-name.pipe';

describe('ShowByNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ShowByNamePipe();
    expect(pipe).toBeTruthy();
  });
});
